
if (typeof jsdataframe === 'undefined') {
  var jsdataframe = require('..');
}

describe('jsdataframe', function() {

  it('should be defined', function() {
    expect(jsdataframe).toBeDefined();
  });

});
