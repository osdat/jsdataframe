
if (typeof jsdataframe === 'undefined') {
  var jsdataframe = require('..');
}
var jd = jsdataframe;

describe('boolean vector methods:', function() {
  "use strict";

  describe('boolVec.and', function() {
    xit('performs elementwise boolean "and"');
  });
});
